% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-from-dir.R
\name{theme_from_dir}
\alias{theme_from_dir}
\title{Create a theme from a directory of template files}
\usage{
theme_from_dir(path, strategy = "overwrite")
}
\arguments{
\item{path}{Path to a directory of template files. Resolved to an absolute
path via \code{\link[fs:path_math]{fs::path_abs()}}. Must exist.}

\item{strategy}{Default merge strategy for all files. Can be overridden
per file via a \verb{_prefab.yml} sidecar. One of \code{"overwrite"}, \code{"skip"},
\code{"union"}, \code{"append"}, or \code{"merge_json"}.}
}
\value{
A \code{prefab_theme} object.
}
\description{
Converts a directory tree into a theme where each file becomes a
\code{\link[=step_file]{step_file()}}. An optional \verb{_prefab.yml} sidecar file in the directory
can override the strategy and template data per file.
}
\examples{
\dontrun{
# Create a theme from a directory of config files
use_theme(theme_from_dir("~/my-template"))

# Compose with other themes
use_theme(r_analysis() + theme_from_dir("~/my-extras"))
}
}
