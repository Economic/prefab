% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch-project.R
\name{launch_project}
\alias{launch_project}
\title{Launch a new project from an editor keyboard shortcut}
\usage{
launch_project(
  theme,
  root = getOption("prefab.project_root", "~"),
  date = FALSE,
  slug = getOption("prefab.project_slug", TRUE),
  open = rlang::is_interactive(),
  label = NULL
)
}
\arguments{
\item{theme}{A \code{prefab_theme} object created by \code{\link[=new_theme]{new_theme()}} or a pre-set
theme function. Required; there is no default, so each key chord names the
theme it applies.}

\item{root}{Directory under which the project is created. Defaults to the
\code{prefab.project_root} option, or the home directory (\code{"~"}) if unset.}

\item{date}{Whether to nest the project under a \code{YYYY-MM-DD} subdirectory of
\code{root} (useful for dated, one-off projects). Defaults to \code{FALSE}.}

\item{slug}{Whether to slugify the entered name into a filesystem-safe
directory name (lower-case, non-alphanumeric runs collapsed to \verb{_}).
Defaults to the \code{prefab.project_slug} option, or \code{TRUE} if unset. When
\code{FALSE}, the name is used verbatim and validated, erroring on characters
that are unsafe in a path component.}

\item{open}{Whether to activate the new project after creating it. Passed to
\code{\link[=create_project]{create_project()}}, following the convention of \code{\link[usethis:create_project]{usethis::create_project()}}:
when \code{TRUE}, the project opens in a new session/window in RStudio/Positron,
or in other editors the working directory of the active session is changed
into the new project. Defaults to \code{\link[rlang:is_interactive]{rlang::is_interactive()}}.}

\item{label}{Optional prompt-window title, used to show which theme a key
chord will apply (e.g. \code{"r_targets + claude_r_targets"}). Because a
\code{prefab_theme} object does not retain the names used to build it, pass a
human-readable label here. Defaults to \code{"New project folder"}.}
}
\value{
The normalized project path (invisibly), or \code{NULL} if the prompt is
cancelled or left empty.
}
\description{
Interactive project launcher: prompts for a project name via
\code{\link[rstudioapi:showPrompt]{rstudioapi::showPrompt()}}, builds a path from \code{root} (and an optional dated
subdirectory), and creates the project with \code{\link[=create_project]{create_project()}}. It is
designed to be bound to a key chord so that a single keystroke scaffolds and
opens a fresh project. The prompt shows the destination directory (including
the dated subdirectory when \code{date = TRUE}), and re-prompts with a hint if the
entered name is invalid rather than aborting.
}
\details{
Bind it in Positron by running it via \code{workbench.action.executeCode.console},
for example in \code{keybindings.json}:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "key": "Ctrl+P N",
  "command": "workbench.action.executeCode.console",
  "args": \{
    "langId": "r",
    "code": "prefab::launch_project(prefab::r_analysis(), label = 'r_analysis')",
    "focus": false
  \}
\}
}\if{html}{\out{</div>}}

Use one chord per theme (mirroring how the built-in themes are composed) to
get a menu of launchers, e.g. a second binding with
\code{prefab::launch_project(prefab::r_targets() + prefab::claude_r_targets())}.

For programmatic (non-interactive) project creation from a known path, use
\code{\link[=create_project]{create_project()}} directly.
}
